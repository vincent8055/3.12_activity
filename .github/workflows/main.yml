name: CIDE for Serverless Application
run-name: ${{ github.actor }} is doing CICD for Serverless Application

on: 
  push:
    branches: [ main, "*"]
  
  jobs:
    pre-deploy: 
      runs-on: ubuntu-lastest
      steps: 
        -run: echo "This job is automatically triggered by a ${{ github.event_name}} event.""